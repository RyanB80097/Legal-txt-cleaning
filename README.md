# Legal-txt-cleaning
A script for regex-based denoising of raw text from TXT and Word documents (typically noisy OCR output), followed by LLM-enabled entity extraction and conversion into structured JSON format.
